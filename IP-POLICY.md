# 知识产权保护策略（IP-POLICY）

EMR Document Editor Engine · Copyright (c) 2026 陈庄旺 · MPL-2.0

本文件说明本项目采用的多层防护方案、**每一层的真实作用边界**，以及明确不做的事。
写清楚「不做什么」和写清楚「做什么」同样重要：它既是给使用者的承诺，也是证明善意、
证明未设置人为障碍的证据。

---

## 1. 范围

**覆盖**：`frontend/` 与 `backend/` 的源码与配置文件、导出产物（HTML / TXT / JSON / 打印）、
源码归档快照、文件头版权声明、许可证与版权归属声明。

**不覆盖**：`knowledge/` 与 `output/` 下的文档（散文资料，另行主张权利）、
`.claude/` 下的工具链配置、第三方依赖（各自适用其上游许可）。

---

## 2. 原则一：文件头是合规与审计基础设施，不是防盗手段

每个源码文件头部都带 MPL-2.0 Exhibit A 通知与 `SPDX-License-Identifier: MPL-2.0`。

它的真实作用是：

- 满足 MPL-2.0 §3.1 对通知保留的要求
- 让 SBOM / 许可证扫描工具能正确识别本项目
- 在发生争议时确立作者身份与许可条款
- 消除「不知道有协议」这一最常见的侵权抗辩起点

**它挡不住任何有意抄袭的人。** 一个 `sed` 就能删掉全部文件头。我们仍然保留它，
因为它的价值在合规与举证，不在阻止复制 —— 认清这一点，才不会对它抱有不切实际的期待。

---

## 3. 原则二：少量隐形标识是取证备用，不是锁

导出的 HTML / TXT / JSON 会携带两类来源标识：

- **可读层**：`<meta name="generator">` 与 `<meta name="emrde-provenance">`，以及 JSON 顶层的
  `_provenance` 字段。任何人打开文件源码都能看到。
- **隐形层**：零宽字符（U+200B / U+200C）编码的溯源串，形如 `1|<版权主体哈希>|<年份>|<许可证>|<引擎版本>`。
  内容**不含姓名、邮箱、设备或用户信息**，只有一个单向哈希（可用自己的署名字符串的 sha256 比对证明归属）。

**它的能力边界**：零宽字符可以被一条正则一次性剥离。它能做到的是 —— 当一份产物外流时，
若标识尚存，就能证明它出自本项目的哪个版本、哪个仓库。它**不能**阻止复制，也**不能**追踪到具体使用者。

> 复制导出的 HTML 全文时，会连带复制不可见字符。这是该方案的固有副作用，已知并接受。

---

## 4. 原则三：提交历史 + 本地归档是核心证据

真正的证据底座不是文件头，而是**时间戳连续、可独立验证**的版本历史。

- **双远程互为旁证**：`origin`（Gitee）与 `github`（GitHub）同时托管，第三方时间戳互相印证。
- **离线归档**：`git bundle` 生成**单文件、含完整历史**的仓库副本，可在**没有任何远程**的情况下
  `git clone` 还原。它挺得过远程账号被删除、平台关停或仓库被清空。
- **校验清单**：`SHA256SUMS` 使事后篡改可被证明；`MANIFEST.txt` 记录 commit、日期、分支、
  文件数与两个远程 URL，把归档与远程标签绑定。

三者合起来才构成证据链：**远程标签证明「当日存在」，归档证明「内容是什么」，校验和证明「有没有被改过」。**

完整操作步骤见第 7 节。

---

## 5. 原则四：明确不做的事

为一款**开源** EMR 引擎加上对抗性保护，是自相矛盾的 —— 那会摧毁可审计性，而可审计性正是本项目的产品价值。

因此，以下做法**明确不做**：

- ❌ **代码混淆 / 压缩 / 名字改写 / 控制流平坦化** —— 直接违反 MPL-2.0 对 Source Code Form 的要求
- ❌ **源码加密 / License-key 门禁 / 保留「高级版」源码** —— 破坏开源承诺
- ❌ **遥测、回传、导出时联网、设备指纹** —— 在病历文档编辑器里做遥测是隐私与合规灾难
- ❌ **「开源但禁止商用」之类的伪开源陷阱条款**
- ❌ **在渲染页面或打印位图上加水印** —— 不改变用户所见是硬约束
- ❌ **让隐形标识承载任何个人信息**

一句话：**为保护开源而背叛开源，是自我否定。**

---

## 6. 许可证决策记录

**选定 MPL-2.0（Mozilla Public License 2.0）。**

- **为什么不是 MIT**：MIT 允许任何人把整套代码闭源拿走并商品化，侵权成本最低，与本策略的目标相反。
- **为什么是 MPL 而不是 AGPL**：MPL 是**文件级** copyleft —— 你修改了本项目已有的文件，那些文件必须继续
  以 MPL-2.0 开源并公开源码；但允许与闭源系统集成、允许商业使用。AGPL 的强传染性会直接劝退医院与企业集成方，
  得不偿失。
- **为什么不用 Exhibit B**：Exhibit B（"Incompatible With Secondary Licenses"）会让代码与 GPL 不兼容，
  在企业法务审核中容易直接卡住。本项目**不适用** Exhibit B，详见 `LICENSE` 末尾说明。

**一次需要记录的更正**：README 从 2026-07-17 起误标为 MIT。当时仓库里**还没有 LICENSE 文件**；
MPL-2.0 全文于 2026-08-04 加入，但 README 未同步更新，造成声明冲突。
2026-09-14 以 MPL-2.0 为准统一。该 MIT 表述是 README 错误而非许可授予：
它从未有 LICENSE 文件背书，没有以它发布过任何版本，且本仓库至今的所有提交均由版权人本人完成，
不存在需要征得同意的第三方贡献。详见 `NOTICE`。

---

## 7. 机制与命令

```bash
# 文件头：幂等写入 / 只读校验
npm run headers:apply
npm run headers:check

# 取证：验证一个疑似外流的导出文件
npm run verify:provenance -- path/to/export.html
node scripts/verify-provenance.mjs -            # 或从 stdin 读取
node scripts/verify-provenance.mjs --selftest   # 编解码自检

# 归档：生成可独立验证的离线快照
npm run archive:snapshot
```

改版权主体 / 年份：只改 `scripts/headers.config.mjs` 里的 `HOLDER` / `YEAR`，
然后 `npm run headers:apply -- --replace`（该模式会整体替换既有头块，不会叠加）。

### 归档快照的验证步骤

```bash
cd <归档目录>
sha256sum -c SHA256SUMS                    # 产物完整性 (改一个字节即失败)
git bundle list-heads *.bundle             # 列出包内全部引用 (无需 git 仓库)
git clone *.bundle /tmp/restored           # 独立还原, 不涉及任何远程
git -C /tmp/restored rev-parse HEAD        # 应等于 MANIFEST.txt 中的 commit
```

> `git bundle verify` 需要在**一个 git 仓库内**运行（它要比对包内前置对象是否齐备），
> 因此不能在归档目录裸跑。要在仓库里校验时：
> `git -C <任一仓库> bundle verify <bundle>`，正常情况下会报告
> `The bundle records a complete history.`

### 建议的异地保存

归档目录默认在仓库之外（`EMRDE_ARCHIVE_DIR`，默认 `D:/emrde-archive`）。
**必须同步到至少一处异地介质**（移动硬盘 / 网盘 / 另一台机器）—— 只存本机磁盘，
磁盘一坏证据就没了，归档的意义随之归零。

---

## 8. 范围外

代码混淆、源码加密、渲染页水印、DRM、贡献者许可协议（CLA）。
CLA **不需要**：MPL-2.0 是 inbound=outbound，接收贡献不会造成许可冲突。

---

## 9. 已知覆盖缺口

诚实列出本方案覆盖不到的地方：

- **打印路径**输出的是 canvas PNG 位图，**没有文本层**，无法承载零宽标识。
  打印窗口 HTML 里的 `<meta>` 只是页面级归属标记，**不是图内溯源**。
- **导出的 PDF / Word** 在导出对话框中列出但**尚未实现**（选了会静默无操作）。
  实现后可在其文档元数据中补溯源。
- **零宽标识可被一次性剥离**。这是设计上接受的取舍：可读 `<meta>` 与文件头才是主层。
  验证工具会对此给出 `MARKER STRIPPED` 判定，说明来源仍可证、但隐形层已失效。

---

## 10. 复核节奏

- **每次 CI 运行**：`npm run headers:check`（新增文件必须带头）
- **每次公开发布 / 对外宣传前**：`npm run archive:snapshot` + 打 `archive/<日期>` 标签并推送双远程
- **每季度**：抽查一次归档的可还原性（跑第 7 节的验证步骤）
